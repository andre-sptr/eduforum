I will implement the AI Chat Card feature with the following steps:

1. **Install Dependencies**:

   * Install `react-markdown` and `remark-gfm` to render the AI's markdown responses nicely.

2. **Create** **`AIChatCard`** **Component** (`src/components/AIChatCard.tsx`):

   * **UI Structure**: A clean, modern card using `shadcn/ui` components (`Card`, `ScrollArea`, `Input`, `Button`).

   * **Chat Interface**:

     * A scrollable area for chat history.

     * User messages aligned to the right (primary color).

     * AI messages aligned to the left (secondary color), rendered with `react-markdown`.

     * Input field and "Send" button at the bottom.

   * **Functionality**:

     * State management for messages, input text, and loading status.

     * API integration with `https://ai.sumopod.com/v1/chat/completions` using the `gpt-4o-mini` model as requested.

     * **API Key Handling**: I will implement a check for `import.meta.env.VITE_SUMOPOD_API_KEY`. If not found, I'll add a small UI input within the card for you to enter the key temporarily (saved to localStorage), ensuring the app works even without an immediate `.env` update.

3. **Integrate into Feed Page** (`src/pages/Feed.tsx`):

   * Import and place the `<AIChatCard />` component immediately above the `<CreatePost />` component.

   * Ensure consistent spacing and styling with the existing feed.

4. <br />

