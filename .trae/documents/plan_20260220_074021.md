I will upgrade the application's loading states to consistently use Skeleton components instead of Spinners (`Loader2`), ensuring a smoother and more modern user experience.

### **1. Create New Skeleton Components**
I will create dedicated skeleton components to match the specific layout of each page:

*   **`src/components/ProfileSkeleton.tsx`**
    *   Mimics the Profile header: Banner, Avatar (circle), Name/Role (text), Bio (multiline text), and Stats (followers/following counts).
*   **`src/components/GroupSkeleton.tsx`**
    *   Mimics the Group list: A grid of cards, each containing a group icon, title, description lines, and a "Join" button placeholder.
*   **`src/components/ChatSkeleton.tsx`**
    *   Mimics the Chat interface: A header bar and a series of message bubbles (alternating left/right alignment) to simulate a conversation loading.
*   **`src/components/GameSkeleton.tsx`**
    *   Mimics the Games list: A grid of game cards with thumbnails and titles.

### **2. Update Pages to Use Skeletons**
I will refactor the following pages to remove full-screen spinners and implement the new skeletons:

*   **`src/pages/Profile.tsx`**
    *   **Current:** Returns a full-screen spinner while fetching profile data.
    *   **New:** Render `ProfileSkeleton` immediately while data loads.
*   **`src/pages/Groups.tsx`**
    *   **Current:** Returns a full-screen spinner.
    *   **New:** Render the Search bar immediately (interactive), and display `GroupSkeleton` in the content grid area.
*   **`src/pages/Chat.tsx`**
    *   **Current:** Returns a full-screen spinner.
    *   **New:** Render the Chat Header immediately (with placeholders if needed) and `ChatSkeleton` in the message area.
*   **`src/pages/PostPage.tsx`**
    *   **Current:** Returns a full-screen spinner.
    *   **New:** Render the Header immediately and use `PostSkeleton` for the post content.
*   **`src/pages/Games.tsx`**
    *   **Current:** Returns a full-screen spinner.
    *   **New:** Render `GameSkeleton` grid.

### **3. Refinement**
*   **`src/pages/Feed.tsx`**: Ensure the initial load uses `PostSkeleton` consistently (already partially implemented, but will verify and refine).
*   **`src/components/ui/skeleton.tsx`**: Verify standard animation consistency.

This approach ensures that the user sees the page structure immediately (Perceived Performance) rather than a blank screen with a spinning icon.