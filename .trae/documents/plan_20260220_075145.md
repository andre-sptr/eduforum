I will implement skeleton loaders for `Search`, `Messages`, `Chat`, and `GroupDetail` to improve the user experience during data loading.

### **1. Update `src/pages/SearchPage.tsx`**
*   Currently, the search page shows `UserSkeleton` and `PostSkeleton` when loading, but the logic can be refined to be cleaner.
*   I will ensure the initial load state (before any results are found) displays a few skeletons to indicate activity if `loading` is true.
*   **Action:** Verify and potentially simplify the loading conditional rendering to ensure skeletons appear immediately upon search submission.

### **2. Update `src/pages/Messages.tsx`**
*   Currently, it shows a full-screen spinner: `<div className="min-h-screen ..."><div className="h-12 w-12 ..."/>...</div>`.
*   I will replace this with a `MessagesSkeleton` component (or inline structure) that mimics the `Tabs` layout (Global, Direct, Groups) and the message list area.
*   **New Component:** `src/components/MessagesSkeleton.tsx` (optional, or inline)
    *   Header tabs skeleton.
    *   List of message previews/users skeleton.

### **3. Update `src/pages/GroupDetail.tsx`**
*   Currently, it shows a full-screen spinner.
*   I will replace this with a `GroupDetailSkeleton` component.
*   **New Component:** `src/components/GroupDetailSkeleton.tsx`
    *   Header banner area skeleton.
    *   Group info (title, stats, buttons) skeleton.
    *   Tabs (Feed/Members) skeleton.
    *   Post list skeleton (reusing `PostSkeleton`).

### **4. Update `src/pages/Chat.tsx`**
*   Currently, it uses `ChatSkeleton` (`if(loading) return <ChatSkeleton />;`).
*   **Action:** Verify `ChatSkeleton` implementation matches the design and ensure no other spinners are blocking the view unnecessarily. (It seems this was already done in the previous step, I will double-check if any refinement is needed).

### **Plan Execution Steps**
1.  Create `src/components/MessagesSkeleton.tsx`.
2.  Create `src/components/GroupDetailSkeleton.tsx`.
3.  Refactor `src/pages/Messages.tsx` to use `MessagesSkeleton`.
4.  Refactor `src/pages/GroupDetail.tsx` to use `GroupDetailSkeleton`.
5.  Review `src/pages/SearchPage.tsx` to ensure optimal skeleton usage (it already uses them, but I'll check for the initial full-screen load).

This will complete the transition to a fully skeleton-based loading experience across the main features.